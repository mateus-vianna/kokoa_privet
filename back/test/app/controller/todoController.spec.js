const jasmine = require('jasmine')
const todoModel = require('../../app/')
describe('todo controller', () => { 
    it('should create a todo', () => { 
        const todoModel({
            name: 'what to do',
            description: 'do some stuff before midnight'
        });
        result = await todoBusiness.createTodo(todomodel);
        expect

    })
})